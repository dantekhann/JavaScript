//Operadores Aritméticos
+ soma
    - subtração
    * multiplicação
    / divisão
    % módulo ou resto da divisão

//Concatenação de strings; 
//Junta várias strings em uma única
'Início do texto ${variável} texto ${variavel_01} + variavel_02} fim do texto'

//Operadores de Incremento e Decremento; aumentam ou diminuem variáveis em uma unidade
++x pré incremento
--x pré decremento
x++ pós incremento
x-- pós decremento

    //Operadores de Atribuição
    = atribuição simples
        += atribuição com soma(ex.x += y ou x = x + y)
            -= atribuição com subtração(ex.x -= y ou x = x - y)
                *= atribuição com multiplicação(ex.x *= y ou x = x * y)
                    /= atribuição com divisão(ex.x /= y ou x = x / y)
                        %= atribuição com módulo(ex.x %= y ou x = x % y)

                            ///Operadores Relacionais (retorna verdadeiro ou falso)
                            > maior que
                                >= maior ou igual
                                    < menor que
                                        <= menor ou igual
                                            == igual
                                            != diferente
                                            === estritamente igual
                                                !== estritamente diferente

//Operadores Lógicos
Conjunção(E lógico) && valida se dois dados são verdadeiros.Escrito uma letra "e" entre duas proposições
Disjunção(OU lógico) || Valida se um dos dois dados apresentados são verdadeiros, ou se ambos são verdadeiros.Escrito com a palavra "ou" entre duas proposições
Disjunção Exclusiva(OU exclusivo) ^ Valida apenas se um dos dois dados apresentados for verdadeiro e o outro dado for falso. "ou" proposição1, ou proposição02
Negação(NÃO lógico)! inverte o valor, verdadeiro vira falso.É escrito como a palavra "não" antes da proposição

